'use strict';

const reversedArray = require('./challenges-01-array-reverse.js');

test('Without using built-in methods available to your language, return an array with elements in reversed order.', () => {
    expect(reversedArray('[5, 4, 3, 2, 1]'));
  });
